// import { ClientDatasource } from "../../../Client/domain";
// import { CustomError } from "../../../domain/entities/error.entity";
// import { Client, ClientDto } from "../../../Client/domain";

// export class ClientDatasourceMongoose implements ClientDatasource{

//     async register(clientDto: ClientDto): Promise<Client> {
//         // const {name, plan_id, subscription_id} = clientDto
//         // try {
//         //     //Validate plan and subscriptions are right
//         //     // return new Client('1',name,plan_id, subscription_id)
            
//         // } catch (error) {
//         //     if(error instanceof CustomError) throw error;
//         //     throw new CustomError('Error creating client')
//         // }
//     }

// }